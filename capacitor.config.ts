import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'seda.easypay.wwww',
  appName: 'easy-pay',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
